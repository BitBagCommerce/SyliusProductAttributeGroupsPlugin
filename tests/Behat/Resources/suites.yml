default:
    suites:
        ui_create_groups:
            contexts:
                - sylius.behat.context.hook.doctrine_orm
                - sylius.behat.context.setup.admin_security
                - tests.bit_bag.sylius_product_attribute_groups_plugin.behat.context.attribute_groups_context
            filters:
                tags: "@create_group && @ui"
